services:
  postgres:
    image: postgres:17
    container_name: n8n-db
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U n8n -d n8n_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - n8n
    environment:
      POSTGRES_USER: n8n
      POSTGRES_PASSWORD: n8n_password
      POSTGRES_DB: n8n_db
    volumes:
      - ./n8n_postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  n8n:
    image: docker.n8n.io/n8nio/n8n
    container_name: n8n
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - n8n
      - frontend
    ports:
      - "5678:5678"
    environment:
      DB_TYPE: postgresdb
      DB_POSTGRESDB_HOST: postgres
      DB_POSTGRESDB_PORT: 5432
      DB_POSTGRESDB_USER: n8n
      DB_POSTGRESDB_PASSWORD: n8n_password
      DB_POSTGRESDB_DATABASE: n8n_db
      DB_POSTGRESDB_CONNECTION_TIMEOUT: 120000
      N8N_HOST: n8n.noorkhafidzin.com
      N8N_PROTOCOL: https
      WEBHOOK_URL: https://n8n.noorkhafidzin.com/
      N8N_ENABLE_API: true
      N8N_BASIC_AUTH_ACTIVE: true
      N8N_BASIC_AUTH_USER: admin
      N8N_BASIC_AUTH_PASSWORD: adminyourpassword123
      NODE_ENV: production
      GENERIC_TIMEZONE: Asia/Jakarta
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./n8n_data:/home/node/.n8n
      - ./local-files:/files

networks:
  n8n:
  frontend:
    external: true
